export const scaleFactor = 35; // Adjusted scaling for better spacing
export const sunRadius = 5;
export const maxOrbitRadius = 50 * scaleFactor; // Ensuring all planets fit
export const canvasSize = maxOrbitRadius * 2 + sunRadius * 2;
export const centerX = canvasSize / 2;
export const centerY = canvasSize / 2;